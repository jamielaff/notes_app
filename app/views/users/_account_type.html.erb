<% if logged_in_as_admin? || user == current_user %>
  <div class="form-group">
    <div class="control-label col-sm-2">
      <%= f.label 'Account type' %><br>
    </div>
    <div class="col-sm-8">
      <%= f.select :is_admin, [ ['team_member', false], ['admin', ] ] %>
    </div>
  </div>
<% end %>